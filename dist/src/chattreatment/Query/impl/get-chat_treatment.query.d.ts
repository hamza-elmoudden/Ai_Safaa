export declare class GetChatTreatmentQuery {
    readonly user_id: string;
    readonly plan_id: string;
    readonly page: number;
    readonly limit: number;
    constructor(user_id: string, plan_id: string, page: number, limit: number);
}
